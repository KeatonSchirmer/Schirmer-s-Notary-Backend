# Add CORS support to your Flask backend
from flask_cors import CORS

# ...existing imports...
from flask import Flask
# ...existing imports...

app = Flask(__name__)
CORS(app, origins=["http://localhost:3000", "http://192.168.0.218:3000"], supports_credentials=True)

# ...existing code...
